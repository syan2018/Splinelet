# 路径 75 跨集合移动回归

本次检查：最新工作文件中「路径 75」是黑色外框下的未闭合 5 段样条，没有构面或修改器引用。旧命令只要部件有修改器就拒绝任何路径移出，属于过度限制。

QA 清单：

- API：真实路径 75 移往全部其他集合，并从有填色/修改器的集合移回；重载后归属正确，所有已有区域几何、颜色、厚度、来源栈与修改器保持不变。
- GUI：普通拖拽、多选拖拽、拖回、撤销/重做、保存重载；接收集合和选中线条在大纲同步可见。
- 异常：真正参与构造的样条被具体依赖提示保护；混合批量失败不能留下半次移动；已停用修改器的引用也需保留。
- 视觉：1440×1000 画布/大纲/属性栏完整可见，拖拽后没有错误遮挡或选中项丢失。

使用隔离浏览器和工程副本测试，原工程不擅自改变路径归属。

结果：14 项命令回归通过，真实路径 75 可往返移动到全部另外 10 个集合，所有已有几何与样式一致。实际浏览器已验证拖入折叠的头饰集合、保持路径选择、撤销/重做及移回；截图保存到 `outputs/path75-transfer/path75-moved.png`。生产构建、TypeScript、新模块 lint 和原有创作回归通过。按用户要求停止扩展/重复测试。

修复同时同步路径的旧 groupId 元数据，并在归属变化（含撤销/重做）时展开目标集合、滚动显示选中线条。画布、大纲与属性栏无新增遮挡；不新增弹窗或操作步骤。真正参与现有构造的线条仍有具体依赖保护，避免批量移动产生半次修改。
