# 原工作区 V4 默认切换验收 · 2026-09-20

分支：`codex/editor-model-v4-plan`。验收代码：`5c49d0c`。结论：**按用户指定的真实工程范围通过，旧写路径切断与原工作区恢复完成。** 此记录验收现有 Studio 的 V4 数据管线，不验收已经撤出的简化候选界面。最终范围采用用户在本轮明确指定的内嵌 Sandrone 打开和编辑，以及 gold 工程补充检查；保留全库工程检查。原 U/A 全矩阵没有逐项重新执行真实 UI 旅程，不将模块测试冒充该矩阵全通过。

## 唯一运行路径

Web 和 Tauri 均经 `studio-entry` 创建 V4 host，再挂载原 Studio；初始化失败直接呈现错误，不回退。`use-studio-project` 只订阅宿主快照。原组件中的可写 Project 状态、旧历史更新、旧保存/恢复队列及两个工作区的 Model Worker 入口已经删除。

编辑、预览提交和撤销进入同一文档事务；`Project` / `Model` 形状仅作为冻结的只读界面投影。保存编码 DocumentV4，恢复使用独立 V4 草稿库。旧工程只能经 `open-project` 单向迁移，升级首次保存写副本；旧数学 helper 和解码代码的保留不构成写入回路。旧候选组件及其测试不在默认入口的可达链，也不作为本次界面验收证据。

## 恢复的行为

- 原布局、样式、画布、工具和高级面板保留。画线、节点/柄编辑、用途切换、移动、编组、撤销、工程打开和保存使用规范命令。
- 树重命名、排序、源线转移到空部件，以及普通闭合/开放线条整理为一个部件使用原子事务；世界坐标与路径用途保留。复杂构造、局部赋值、依附或制造绑定不被偷偷丢弃；不能安全合并时明确拒绝。
- 对象高度与依附、打印分层启用和归属、分区端点接合预览/接受/开关恢复；预览连接取自实际求值诊断。
- 高级构造支持候选子集提交、区域删除与引用检查、重绑定、独立重复浮雕贡献、区域/体块名称与属性、零件归属、曲线公差和制造清理。布尔与分区建立独立派生部件并保留输入；提交复用已预览定义，不从多边形结果反建来源。
- 修改器卡片显示真实可用能力。同域线性链可增删排序；分叉、精确作用范围和跨域调整受保护。原界面本无制造零件删除按钮，本次没有把它扩展成新功能。

## 制造清理修复

原 Sandrone 含 `manufacturingMM: 0.02`。此前导入丢失此字段，因此旧的 Body PASS 不能证明等价。现在导入保留为 `manufacturing.cleanupRadiusMM`，仅在实体阶段清理，不改 Sketch。

恢复该值后曾触发 120 秒超时。修复包括按高度区间构建截面、合并根查找压缩、退化三角形邻接索引，以及只对无效网格执行昂贵的修复。规范网格使用 `cleanMesh(raw, true)` 保留 Manifold 合并拓扑，未改成按坐标焊接。

本机 Node 实测两份原始工程均约 5 秒得到 ready：内嵌项目 19,930 三角形、69 材料分区；gold 21,098 三角形、70 材料分区。均为单个有效闭合实体，异常边和零面积面为 0，材料总体积与实体一致。性能数字仅属于本次机器/样例快照。回归同时覆盖原始 0.02 设置与显式禁用清理。

## 验证记录

- 原 Studio 完整浏览器回归：`outputs/v4-qa/original-v4-cutover-2026-09-20.log` PASS。实际界面、样式、Sandrone 编辑/撤销/保存重开、V4/旧文件/内嵌示例打开、错误文件保护，以及高级面板 69 区域/69 体块和清理后的有效 Body 均有断言；浏览器错误数组为空。
- 高级构造、属性、修改器、连接、来源转移、对象整理和打印分层的专项回归通过。承托部件跨对象输入的排序能力检查曾导致界面报错，修复后 support 与原界面回归通过。
- `pnpm check:all`：`outputs/v4-qa/check-all-cutover-2026-09-20.log`，退出码 0。129 项隔离单元测试、TypeScript、lint、格式、Rust format/check、Web 和桌面前端生产构建均通过。
- 原生 release：`outputs/v4-qa/release-cutover-2026-09-20.log`，退出码 0。实际程序为 `src-tauri/target/release/splinelet.exe`。
- 原生默认入口：`outputs/v4-qa/native-default-entry-2026-09-20T03-13-43.531Z-54832/manifest.json` PASS。实际 Ctrl+S 写入绑定的测试工程；内嵌 Sandrone 与 gold 均完成控制柄编辑、撤销、再次编辑、导出 V4 副本和原生打开重载，两个原件 byte-compare 不变。
- 原生测试暴露底图切换时序和测试等待错误：就绪状态现绑定当前底图，测试对 API Promise 解析后的布尔值轮询，不再把 Promise 本身当作等待成功，也不靠固定延时。此增量通过类型/lint、原生重建与上述原生验收；Web 构建（`web-cutover-final-2026-09-20.log`）、全库格式和采用正确异步等待的完整原界面浏览器回归再次通过。

本轮真实原生验收使用本机专用进程和临时副本。原件只读；不要求 VM，也不操作用户已打开的文件。既有原生脚本的浮雕/挤出偏好断言不混为厚度交互验收；厚度由规范命令及原界面浏览器断言覆盖。
