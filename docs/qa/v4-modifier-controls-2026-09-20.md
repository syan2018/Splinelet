# 原修改器参数适配验证

日期：2026-09-20。范围：原界面控件所需的 V4 读取与命令适配，不作为原 UI 已接入或原生保存验收。

`modifier_update` 现在支持名称、启用状态，以及镜像、阵列、偏移、布尔的对应标量字段。镜像角度和中心按原控件的世界坐标语义转换；阵列角度保持相对步进。只修改构造参数，不回写派生曲线。

读取投影提供真实求值、可编辑字段、驱动字段和诊断。Parameter 或 expression 驱动值可显示，但普通数值编辑不能解除绑定；求值失败不伪造零值。来源、目标、接边及构造增删排序仍需单独的明确命令接入。

`test-v4-modifier-intents.mjs` 覆盖字段、锁、坐标和驱动保护；`test-v4-modifier-control-runtime.mjs` 通过实际会话证明旋转部件中的中心/角度修改、原曲线不变、输出变化、一次撤销和过期视图拒绝。

`pnpm check:all` 退出码 0：89 项 hermetic 单元测试、类型、lint、格式、Rust 格式与编译、Web 和桌面前端构建通过。日志为 `outputs/v4-qa/check-all-modifier-controls-2026-09-20.log`。

默认入口仍为原 UI 的旧后端。本次没有替换界面，也没有完成原工作区控件、文件会话和模型工作区的 V4 接线。完整重构 goal 继续进行。
