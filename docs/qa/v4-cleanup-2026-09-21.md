# V4 编辑收尾验收（2026-09-21）

基线为 `0c790d1`，本记录对应其后的编辑收尾清理。任务范围见[检查点](../../tasks/v4-cleanup-2026-09-21/README.md)，当前语义见[构造流水线](../architecture/construction-pipeline.md)、[修改器](../modifiers.md)和 [Agent API 5](../agent-api-2026-09-20.md)。

## 清理内容

- Studio host 统一持有当前文档求值会话。GUI 和 Agent 的同一修订、预览帧、阶段集合共享进行中请求与完成缓存；预备候选继续使用同一后端，但不污染当前缓存。取消、更新、换工程和迟到结果都有身份检查。
- Creation cell 区分未启用、制造排除、阶段未求值和真实失败；空浮雕成员不再一律标记 blocked。
- Join 写入验证端点、实例与相对选择器语法；保留合法但未完成的拓扑。端点菜单显示段号和实例，局部图高亮 A/B 曲线与端点，使用求值器相同的实例选择逻辑。
- Group 单选、多选和 Group/Shape 混选分别呈现。批量解组明确按根组执行，删除的 Group 不留在选区，仍存在的 Shape 和内层组继续保留。
- API capabilities 与工具契约收敛，Studio 注册、WebMCP schema、只读提示和兼容写保护从统一 catalog 派生；移除重复清单及失效文档示例。
- 原 Studio、最终面和组选区验收接入公共浏览器 runner；增加 `test:browser:studio` 与串行 `test:browser:all`。每个 case 使用隔离服务和全新 browser/context。

## 验证

- `pnpm check:all` 中类型、lint 和 **133 项 hermetic 单元测试**通过，首次停在并行编辑的四个浏览器文件格式检查。日志：`outputs/v4-qa/cleanup-check-all-2026-09-21.log`。
- 整合后的 `pnpm typecheck`、`pnpm lint`、`pnpm desktop:format:check`、`pnpm desktop:check`、`pnpm build:all` 通过。双端构建日志：`outputs/v4-qa/cleanup-builds-2026-09-21.log`。
- 当前 Web 生产构建的 `agent-spline-authoring` 通过：公开 API 5 创建样条/镜像/阵列/Join/Fill，原杯体保留、错误隔离、严格导出、GUI/API 身份、修改器编辑、保存重开和实际 3MF。证据：`outputs/v4-qa/cleanup-agent-authoring-2026-09-21/manifest.json`。
- Studio `final-preview` 通过：默认最终面、显示选项、四组 Join 重复端点高亮、构面、Group 移动/换父级/撤销。已查看局部端点图，金色 A 与青色 B 对应当前连接。证据：`outputs/v4-qa/cleanup-studio-final-preview-2026-09-21/manifest.json`。
- Studio `scene-group-selection` 通过：混选保留部件、祖先去重、多个独立组解组和删除后的选区。证据：`outputs/v4-qa/studio-scene-group-selection/manifest.json`。
- Studio `original-studio` 完整旅程通过：Sandrone 打开、样条/区域交互、高级模型面板、撤销、保存、新旧文件与错误文件保留。新图测试改为等待当前重算、点击实际区域内部，避免使用过期画布坐标。证据：`outputs/v4-qa/studio-original-studio-corrected/manifest.json`。
- 浏览器 runner 定向测试与最终 `pnpm format:check` 通过；本次修改文档的 60 个本地链接/标题锚点及 `git diff --check` 通过。上述首次格式阻断已解除，所需检查、Rust 检查和双端构建均完成。

## 验证边界

Studio suite 使用源码 fixture 与真实 Worker，不等同于生产包验收；生产包另由上面的 Agent 创作旅程覆盖。本轮没有运行完整 legacy 浏览器 suite 或 Tauri 原生交互/发布构建。原 Studio 长流程保留自身的等待预算，runner 的超时参数不构成整个用例的总时限。旧文件单向导入和公开兼容调用继续保留，未删除仍有真实用途的兼容边界。
