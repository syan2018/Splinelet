'use client';

// Both hosts retain the established editor UI. The data-model migration must
// integrate behind this workspace; query parameters cannot replace the UI.
export { default } from './studio-app';
