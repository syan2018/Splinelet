# 工作包 5：验证作者区域模型

日期：2026-09-26。状态：实验归档，不纳入最终产品接入。共享边界只是一种可选源修改器输入，不作为全部旧工程的强制替代模型；产品改由[链修复工作包](06-chain-recovery.md)交付。语义以[修改器链设计](../../docs/architecture/region-identity-redesign-2026-09-26.md)为准。以下未勾选项保留原实验范围，不代表另有一条待接入的产品管线，也不将未实施实验记为完成。

## 最小验证

- [x] 两个不同颜色、厚度的区域引用同一边界曲线和共享接点，曲线内部包含多个贝塞尔段。
- [x] 删除内部样条锚点时保持区域连接关系；实际填充范围随形状变化，共边几何完全一致。
- [x] 精确拆段和表示反转不改变填充范围，不使用历史求值面、选择器或几何相似度。
- [x] 移动共享接点同时更新相关边；几何穿越不隐式焊接或合并区域。
- [x] 不同属性的区域合并必须有明确策略；拒绝时作者文档完全不变。
- [ ] 冷求值、保存重开、撤销恢复作者对象；纯求值不修改文档。

运行 `node scripts/validation/prototype-author-regions.mjs`。当前只支持每区域一个简单闭环，覆盖以上前五项及纯求值不写文档。额外用几何相交/并集核对两个区域没有重叠或缝隙、实际红蓝范围正确；不是仅检查 ID 没变。保存/撤销和完整填充语义尚未接入。

隔离原型仅用于验证这一数据边界，不能替代现有 UI、文件迁移和实体管线。产品 schema 和命令接入仍未定案，不可将这些勾选当成整体工作包完成。

## 产品接入

- [ ] 源几何只有一份，区域只引用共享边界；不缓存第二份可编辑 polygon。
- [ ] 控制节点编辑与拓扑接点操作在命令层明确分开，保持现有页面布局及源编辑入口。
- [ ] 临时选区、作者区域、构造结果集合、实例使用不同目标类型。
- [ ] 外观与浮雕启用/厚度分开；一次上色不暗中创建子面制造依赖。
- [ ] 程序式操作仍按明确输入和参数工作；选择结果碎片时保留明确的参数关联或转换语义。
- [ ] 默认编辑链不再调用区域身份匹配或为每次控制点移动重新建立区域作者关系。

## 真实工程与清理

- [ ] 在原始与用户编辑后的文件副本上验证迁移，原文件哈希不变。
- [ ] 迁移报告说明新增接点及编辑语义变化；不无条件焊接全部隐式交点。
- [ ] 重放原头发失败操作，不缩小位移、不另选安全节点；核对物理属性、构造输入和制造结果。
- [ ] 保留源输入与派生求值解耦的性能修复，测量真实交互和下一次输入响应。
- [ ] 删除被替代的默认身份/契约链，更新格式、API、脚本和用户文档。
- [ ] 完成 AGENTS.md 的全部仓库门禁与隔离浏览器验收，再更新公开示例和交付结论。
