# 工作包 6：修改器链与修复协议

日期：2026-09-26。状态：实现中，尚未整体交付。架构以[修改器链设计](../../docs/architecture/region-identity-redesign-2026-09-26.md)为准。

## 第一切片

- [x] 复用 Program/PortRef、逐节点依赖缓存和 prepare/commit/undo，停止扩充全局面身份匹配。
- [x] 每端口保留一份已提交的成功结果；区分当前结果与历史显示，拒绝旧 epoch/revision/preview version。
- [x] partial domain 请求不误删未请求快照；换工程、删除节点及 dispose 释放引用。
- [x] 输入诊断保留具体端口、位置及原引用；输入解析异常局部化。
- [x] 基于当前作者 ID 的参数编辑不等待全工程求值，不传旧 scene。
- [x] 明确替换一个输入：候选预览不发布到当前会话，提交有修订保护、可一次撤销，取消不修改文档。
- [ ] 隔离浏览器验证修改器卡片内输入修复、历史快照展示和参数编辑。
- [ ] 完成该切片的全库检查及双端构建，记录限制。

运行时与事务回归：`node scripts/tests/unit/test-chain-snapshots.mjs`、`node scripts/tests/unit/test-modifier-chain-recovery.mjs`。后者使用真实 V4/V5 编辑器、算子和求值服务，验证无关分支保持可用、正常修改不重绑、断链保留快照、候选取消、提交撤销及过期拒绝。

## 后续清理，不能冒充已完成

- [ ] 将头发分区中的隐式 Join/Extend 和局部选择变为有明确输入契约的修改器。
- [ ] 局部属性引用 Select/裁剪结果；整体与实例消费者不依附临时 polygon 下标。
- [ ] 将 Appearance、Relief、Placement、Cleanup 逐步纳入同一类型化依赖计划，清理固定编排。
- [ ] 增加声明约束的候选求解、歧义解释及手动重新指定条件；不默认按几何相似度改作者状态。
- [ ] 实现显式 Bake/Snapshot source 与资源持久化；当前历史快照仍仅在内存用于查看。
- [ ] 迁移副本并重放原头发失败操作，核对属性范围、链关系、制造结果、保存重开及撤销。
- [ ] 清除已替代兼容链，完整通过仓库门禁和真实交互验收，再交付修复示例。

第一切片不解决全部局部区域选择问题；它提供可观察、可修复的基础，而不是以“失效就暂停”作为最终产品能力。
