# 工作包 6：修改器链与修复协议

日期：2026-09-26。状态：完成。架构以[修改器链设计](../../docs/architecture/region-identity-redesign-2026-09-26.md)为准。

## 第一切片

- [x] 复用 Program/PortRef、逐节点依赖缓存和 prepare/commit/undo，停止扩充全局面身份匹配。
- [x] 每端口保留一份已提交的成功结果；区分当前结果与历史显示，拒绝旧 epoch/revision/preview version。
- [x] partial domain 请求不误删未请求快照；换工程、删除节点及 dispose 释放引用。
- [x] 输入诊断保留具体端口、位置及原引用；输入解析异常局部化。
- [x] 基于当前作者 ID 的参数编辑不等待全工程求值，不传旧 scene。
- [x] 明确替换一个输入：候选预览不发布到当前会话，提交有修订保护、可一次撤销，取消不修改文档。
- [x] 隔离浏览器验证修改器卡片内输入修复、历史快照展示、取消、确认和撤销；pending 参数命令由运行时测试覆盖，未声称完成浏览器延迟求值测试。
- [x] 执行该切片的全库检查及双端构建；早期格式门禁失败已在最终整理中修复，全库格式通过。

运行时与事务回归：`node scripts/tests/unit/test-chain-snapshots.mjs`、`node scripts/tests/unit/test-modifier-chain-recovery.mjs`。后者使用真实 V4/V5 编辑器、算子和求值服务，验证无关分支保持可用、正常修改不重绑、断链保留快照、候选取消、提交撤销及过期拒绝。

## 完整链路

- [x] 头发接边拆成 `curve-endpoint-attach`；分区后的局部属性使用 7 个显式 `region-select`。
- [x] 选择组按完整属性确定；具名结果可由多面组成，同组去重合并、跨组重叠拒绝，不以临时面下标保持属性。
- [x] Appearance、Relief、Placement、Cleanup、Body 从作者表编译依赖图，通用执行器按请求闭包计算；移除后段固定调用顺序。
- [x] 兼容选择条件可手动预览重选；缺失/歧义/占用明确诊断，无最近面自动修复。
- [x] Bake 创建独立 `region-snapshot-source`，随 `.spl` 保存、可撤销、不自动发布；历史世界坐标重表达避免偏移。
- [x] 副本重建保留源曲线、厚度、颜色及制造设置，原失败节点同幅拖动、控制柄、整线、撤销、重开与实体导出通过。
- [x] `pnpm check:all` 全部通过（163 项单测及双端构建），公开示例已替换，原始两份文件哈希不变。

旧格式 reader/导入核对和已有明确边界选择保留兼容，不为删除代码而破坏旧文件。它们不能在 V5 中回写 outputContract 或猜测新的属性目标。任意拓扑变化仍可能需要用户修复；本轮交付的是明确选择、可编辑的条件和可撤销的修复流程，不承诺替用户猜对每次几何改变的意图。

具体通过范围、发现并修复的问题和测量边界见[验收记录](../../docs/qa/modifier-chain-recovery-2026-09-26.md)。
