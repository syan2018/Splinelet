# 工作包 4：增量求值与收尾

日期：2026-09-26。状态：局部性能修复已实现，最终验收未完成；作者模型需按第一性复审调整。

- [x] 按实际依赖、上游 generation、局部定义和精度策略构造缓存键，不为每算子序列化全部定义。
- [x] 内建纯求值命中缓存时复用冻结 DTO，避免先 clone/变换；自定义 resolver 保持输入值检查与内存隔离。
- [x] 跨 Shape、关系、世界坐标与实例失效用独立冷求值对照；未变化组件不重算。
- [x] 显式检查/导出与最新预览请求分开，后者不吞掉前者。
- [x] V5 正常区域链退出递归身份 key、旧契约和隐式 lineage 继承；根因诊断沿实际依赖传播，未决绑定阻止错误制造。
- [ ] 真实示例编辑、双端生产构建、全部仓库门禁与隔离浏览器验收；按完成情况记录，不能将静态导出通过当作编辑体验通过。

实施调整：本次通过局部源预览、依赖缓存和冻结 DTO 共享处理已确认瓶颈，没有引入通用 Worker 显示 patch 协议。后台显示/显式检查仍消费完整快照，冷求值与消息序列化仍有成本；不得把这部分写成已完成的 patch 优化。全库格式历史基线与源输入延迟预算也在[验收](../../docs/qa/region-repair-2026-09-26.md)单列。
