# P00 · 架构合同、公共整合与默认切换

建档：2026-09-19。**这是一个分发工作包；T 编号是包内验收检查点，不要求为每个 T 新开执行任务。**

- 包负责人 / 验收者：主代理 / 独立合同审阅
- 建议角色：主代理 / 整合负责人
- 执行状态：见下面各检查点；准备阶段实施中
- 起始提交 / 合同版本：1fc9902 / v1
- 总控：[范围、合同、最快可行调度与门槛](README.md)
- 设计依据：[架构方案](../../docs/architecture/editor-model-review-and-refactor-2026-09-19.md)
- 仓库约定：[AGENTS.md](../../AGENTS.md)

## 背景与要交付的改变

当前场景归属、源编辑、Worker 回写和 React 根状态耦合；若按文件各自改造，会产生两份可写工程、接口循环和交互术语膨胀。本包负责把已评审的对象组织与分阶段管线落实成合同，并持续将真实模块接入同一个编辑会话。

独立源定义和结果域在同一编辑器内贯通；用户仍从画线、上色、厚度开始。最终默认 V4，旧格式只在导入边界解释。

## 在整条管线中的位置

输入是架构方案与各包签收交付；输出是版本化合同、公共装配提交、G0–G5 证据及最终切换。I00 在本包内持续执行，不是另一个分发文档。

承担架构、坐标、几何关系和并发决策的最终复核；公共文件串行装配。领域算法由相应包提供，不在根组件复制实现。

所有包共同守护：部件身份不依赖输出类型/数量；源定义是唯一权威，求值结果只读；普通用户的默认概念只有部件、线条、区域、颜色和厚度，高级业务按需展开。技术正确与默认体验同时验收。

## 如何分发本包

先读本页背景、总控第 1–2 节及相关代码入口；开工前记录已验收的上游提交和合同版本。只启动依赖已就绪的检查点，不需等前一工作包全部做完，也不能跳过本检查点依赖。负责人可连续完成多个检查点，或在总控许可的非重叠范围内分给临时协作者。

- [T00 冻结模块合同与默认体验边界](#t00)：可从准备阶段开始
- [T22 默认切换、旧写入退役与当前文档更新](#t22)：[T21](01-validation-2026-09-19.md#t21)
- [I00 持续整合](#i00)：随 T00/T01 启动，贯穿全部阶段

交付物先做覆盖面的最小验证，再交 P00 接线；同一待验收提交上的全量检查证据可以被多个检查点引用，不重复跑同一批构建。各检查点仍需自己的反例和语义验收。全部小项写完之前，下游可以使用已单独签收的检查点交付。

禁止修改未授权公共文件或其他执行者的工作；需扩范围时给出具体文件、理由、消费者影响，由 P00 登记后执行。

<a id="t00"></a>

## T00 · 冻结模块合同与默认体验边界

- 状态：完成
- 执行者 / 验收者：主代理 / 独立合同审阅
- 前置：无；G0 准备项
- 下游：[T02](02-document-and-persistence-2026-09-19.md#t02)
- 覆盖原工作包：R0
- 验收复核：主代理确认架构合同，另一执行者按例子检查可实现性。

### 目标与代码背景

把现有设计转成可独立实现的版本化合同；决定数据含义与依赖方向，不在本任务实现模型或控件。

现有代码入口（用于理解与复用，不自动获得写权限）：

- [docs/architecture/editor-model-review-and-refactor-2026-09-19.md](../../docs/architecture/editor-model-review-and-refactor-2026-09-19.md)
- [src/lib/project.ts](../../src/lib/project.ts)
- [src/lib/modifier-types.ts](../../src/lib/modifier-types.ts)
- [src/lib/modifier-stages.mjs](../../src/lib/modifier-stages.mjs)

### 可写范围

- tasks/editor-model-v4-refactor/contracts-2026-09-19.md（新增）

上述未存在模块均为拟新增路径。本检查点只修改此范围及执行记录；公共接线交 [P00](./00-architecture-and-integration-2026-09-19.md) 的 I00 处理。

### 分步交付

1. 定稿 C01–C08 合同，包含类型判别、所有权、端口、单位/框架、状态、错误与函数签名；逐项消除“由调用方猜测”的字段。
2. 用空部件、闭合轮廓、多来源分区、1/8 母线、纯引用部件五个数据例子走通全部结果域；写出简单动作如何编制同一 Program。
3. 冻结关系首版白名单、编辑自由量、输出绑定时机、逐区域浮雕优先级和失败状态；高级编辑与普通操作的作用范围共用定义。

按交付物提交小改动，不等整包所有检查点做完再一次提交。

### 验收

覆盖：**A18–A20、A23；U01–U06 的设计前置**；最终标准见架构方案第 10 节。

- [x] 对照 U01–U06 做步骤走查：闭合、上色、厚度、预览、导出没有端口或技术目录必答设置。
- [x] 合同中的每个结果域至少有一个合法、空、失效例子；Document 不含求值网格/缓存，运行时结果不得成为第二份可写源。
- [x] 由主代理复核后填写合同版本及评审证据；未定稿不得由下游自行补语义。

本项以数据示例/用户步骤走查签收，同时检查 Markdown 链接和 `git diff --check`。不以创建文档代替合同评审。

### 执行与验收记录

- 认领人 / 时间：主代理，2026-09-19
- 起始提交 / 合同版本：1fc9902 / v1
- 实现提交 / 关键变更：本次合同提交；固定 C01–C08 字段、类型、引用框架和服务接口
- 实际命令、结果、环境及证据路径：合同独立走查及五项修正复核通过；oxfmt、git diff --check
- U/A 编号与 fixture：文档五例覆盖 U01–U06 设计前置、A18–A20/A23 合同；不代表代码验收
- P00 接线提交 / 合同或范围变更：跨 owner 限制、引用入口、port 身份、关系框架和类型别名已定稿
- 遗留问题 / 阻塞条件：无；开始 T02
- 验收人 / 日期 / 结论：独立合同审阅者与主代理，2026-09-19，可冻结

<a id="t22"></a>

## T22 · 默认切换、旧写入退役与当前文档更新

- 状态：未开始
- 执行者 / 验收者：未分配
- 前置：[T21](01-validation-2026-09-19.md#t21)
- 下游：P00 最终收口
- 覆盖原工作包：R8
- 验收复核：主代理在最终提交上签收；未通过G4不能标完成。

### 目标与代码背景

在完整验收后让生产编辑器只使用V4，并清除旧写入口；旧格式仅保留受控导入与测试对照。

现有代码入口（用于理解与复用，不自动获得写权限）：

- [src/components/studio/studio-app.tsx](../../src/components/studio/studio-app.tsx)
- [src/components/creation/creation-workspace.tsx](../../src/components/creation/creation-workspace.tsx)
- [src/components/modeling/model-workspace.tsx](../../src/components/modeling/model-workspace.tsx)
- [README.md](../../README.md)
- [docs/README.md](../../docs/README.md)
- [docs/project-format.md](../../docs/project-format.md)
- [docs/creation-2026-09-19.md](../../docs/creation-2026-09-19.md)

### 可写范围

- I00许可下的共享入口/旧调用退役
- README.md、docs/ 当前指南与 scripts/README.md
- tasks/editor-model-v4-refactor/acceptance-2026-09-19.md 的最终签收

上述未存在模块均为拟新增路径。本检查点只修改此范围及执行记录；公共接线交 [P00](./00-architecture-and-integration-2026-09-19.md) 的 I00 处理。

### 分步交付

1. 在保留原有 `studio-app.tsx` 布局、样式、工具和基本行为的前提下，让启动/新建/打开/恢复使用 V4；只在版本分派处处理旧文档。切换的是数据与命令实现，不是另一套编辑器界面。2026-09-19 的简化候选界面入口已撤销，不能作为 T22 的交付基础。
2. 按退出清单删除旧GUI/API写数组、normalize-as-read、后台bind写回及compileCreation回路；旧数学内核可保留，旧模型解释不得混入新编辑器。
3. 当前产品文档、API样例、测试命令和迁移说明改为实际行为；保留原V3文件及报告，明确旧客户端不能无损读写V4。

按交付物提交小改动，不等整包所有检查点做完再一次提交。

### 验收

覆盖：**全部 U/A；R8 最终退出条件**；最终标准见架构方案第 10 节。

- [ ] 生产调用图搜索证明只在import/test对照出现旧模型推导；剩余引用逐条分类，不用一次全局删除替代核对。
- [ ] 切换后重新跑全量checks、V4浏览器旅程、版本打开/保存和独立原生烟测；新增失效回退功能开关并保留文件，不反向写成V3。
- [ ] 默认界面无新增技术目录/模式门槛，所有U/A证据对应最终切换提交。

验证命令：

```sh
pnpm check:all
pnpm test:browser --suite v4 --target web
pnpm test:browser --suite v4 --target desktop-frontend
```

上述 `test-v4-*.mjs` 是拟新增测试文件，交付后即可直接用 Node 运行，并由现有 `pnpm test` 自动枚举；Node 单测不等待浏览器 runner。只有列出的 `pnpm test:browser` 命令需要 P01/T01 先交付入口。实现任务还须引用待验收提交上的 `pnpm check:all` 结果；计划本身不构成通过证据。

### 执行与验收记录

- 认领人 / 时间：
- 起始提交 / 合同版本：
- 实现提交 / 关键变更：
- 实际命令、结果、环境及证据路径：
- U/A 编号与 fixture：
- P00 接线提交 / 合同或范围变更：
- 遗留问题 / 阻塞条件：
- 验收人 / 日期 / 结论：

<a id="i00"></a>

## I00 · 持续整合与阶段签收

- 状态：执行中
- 执行者 / 验收者：待指定；整体架构与风险判断由主代理负责
- 前置：从 T00/T01 开始；每次装配只依赖该次已验收模块
- 最终完成条件：T22 与 G5 完成

### 可写范围与交付

持续维护本目录合同/总控/验收索引。公共接线包括：
`src/components/studio/studio-app.tsx`、
`src/components/creation/creation-workspace.tsx`、
`src/components/modeling/model-workspace.tsx`、
`src/lib/model-worker.ts`、
原 `project-format/persistence/API` 入口、
`package.json`/`pnpm-lock.yaml`、
Vite 配置、Web/桌面入口及必要的平台装配。

已有纯几何/实体/格式内核如需签名补丁，按逐文件登记纳入；并非授权顺便重写全部模块。默认不改 Rust 权限、bundle、兼容静态路径或用户数据。

1. 记录每个模块的合同版本、依赖、写入者及消费者；冻结 C01–C08，主代理处理跨模块几何/坐标/并发决策。
2. 为 P01 串行注册浏览器依赖/命令；每个域服务完成即装配真实实现，未完成阶段明确 unavailable。
3. 建立隔离 V4 测试入口；将会话、Worker、投影、命令、源画布、工作区、保存、API 逐次接入，一次整合一个行为边界。
4. 在 G1 接通无界面主链，在 G2 从正常 UI 验证简单流程，在 G3 接通迁移/持久化；最后由 P01 独立验收，不等末尾做一次大合并。
5. 持续维护旧调用退出清单，G4 后执行 T22 默认切换和退役，记录最终 G5 证据。

### 每次接线验收

- [ ] 应用只持有一种可写 Document；不通过旧 paths/features 接收新结果。
- [ ] GUI/Worker/导出使用同一提交的指定阶段；无后台 bind 写回或隐式切换来源管线。
- [ ] 当前提交相关最小测试与仓库全量检查通过；改 Worker/wasm 或入口包含两端生产构建。
- [ ] 简单用户路径无需技术配置；接线不引入第二套选区/保存/历史状态。
- [ ] 公共文件补丁可单独审阅；上游合同变化已通知并重验受影响消费者。

### 共享写入与整合记录

| 日期   | 文件/范围 | 来源检查点 | 执行者 | 合同版本 | 整合提交 | 验证与结论 |
| ------ | --------- | ---------- | ------ | -------- | -------- | ---------- |
| 待开始 | —         | —          | —      | —        | —        | 未执行     |

最终由主代理依据 G0–G5 和全部 U/A 证据签收。验收材料、阻塞条件及签收日期在本节与验收索引关联。

### 实际 Worker 接通签收

T14 先签协议/会话核心。I00 在 T08 后接平面服务，在 T11 后接实体服务；分别记录真实 Worker 请求/结果、Fill 失败时的当前曲线、BodySet 与导出的同 revision、乱序拒绝。相关自动断言放拟新增 `scripts/tests/unit/test-v4-worker-integration.mjs`，浏览器在 P01 runner 中以新 context 实际加载 Worker/wasm。两端生产构建和真实服务测试通过，才允许 G1/G2 全链签收。协议 fixture 单测不能替代这些证据。

- 状态：未开始
- 平面服务提交 / 证据：待填
- 实体服务提交 / 证据：待填
- 双端构建 / 浏览器 Worker 结果：待填
- 验收人 / 日期 / 结论：待填
