# V4 复审修复

对应 [复审记录](../../docs/qa/v4-refactor-review-2026-09-20.md)，在原 Studio、同一 V4 编辑事务与求值器上修复，不替换界面，不修改用户工程。

- [x] R1：保留当前求值的独立分支，严格阻断不完整制造/导出；覆盖附着和层依赖。
- [x] R3：对象 Z 编辑仅修改默认及显式放置覆盖，保留继承关系。
- [x] R2：原 host 接入 API 5，完成显式 Join/Fill 创建入口与图样验收。
- [x] R4：原作品树展示场景 Group，支持层级选择、移动和 keepWorld 换父级。
- [x] 同步当前文档，执行针对性回归与 `pnpm check:all`，记录实际通过和限制。

底层语义和跨模块实现由主代理处理。几何结果只来自当前 revision；预览保留独立分支不代表允许残缺导出。
